apiVersion: v1
kind: Secret
metadata:
  name: xai-engine-secrets
  namespace: ibis-x
type: Opaque
# Placeholder file: real values are injected at `make update-secrets` time
# by scripts/development/update-local-secrets.py, which reads the root .env.
# NEVER commit real base64-encoded secrets here.
data:
  database-url: REPLACE_WITH_DATABASE_URL
  # MinIO dev defaults — overridden by overlays/azure in production.
  minio-access-key: bWluaW9hZG1pbg==
  minio-secret-key: bWluaW9hZG1pbg==
  openai-api-key: REPLACE_WITH_OPENAI_API_KEY
