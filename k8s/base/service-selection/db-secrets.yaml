apiVersion: v1
kind: Secret
metadata:
  name: db-secrets
  namespace: ibis-x
type: Opaque
# Placeholder file: real value is injected at `make update-secrets` time
# by scripts/development/update-local-secrets.py, which reads the root .env.
data:
  database-url: REPLACE_WITH_DATABASE_URL
